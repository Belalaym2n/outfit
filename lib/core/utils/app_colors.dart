import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors{
  static const Color primaryColor = Color(0xFF0D0D0D);
  static const Color textWarn    = Color(0xFF8C3A2B);
  static const Color bg         = Color(0xFFF7F6F3); // warm off-white
  static const Color surface    = Color(0xFFFFFFFF); // card / field bg
  static const Color surfaceAlt = Color(0xFFF2F1EE); // secondary surfaces
  static const Color divider    = Color(0xFFE8E6E1); // hairlines, borders
  static const Color dividerFoc = Color(0xFF1A1917); // focused field border

  static const Color textHigh   = Color(0xFF1A1917); // headlines
  static const Color textMid    = Color(0xFF6B6861); // body / labels
  static const Color textLow    = Color(0xFFAEABA4); // placeholders
  static const Color textLink   = Color(0xFF1A1917); // tappable links

  static const Color ink        = Color(0xFF1A1917); // primary CTA
  static const Color inkShadow  = Color(0x331A1917); // button shadow
  // Derived surface layers
  static const Color surface0 = Color(0xFF0D0D0D);
  static const Color surface1 = Color(0xFF141414);
  static const Color surface2 = Color(0xFF1C1C1C);
  static const Color surface3 = Color(0xFF242424);

  // Text hierarchy
  static const Color textPrimary = Color(0xFFF5F5F5);
  static const Color textSecondary = Color(0xFF8A8A8A);
  static const Color textTertiary = Color(0xFF4A4A4A);

  // White spectrum
  static const Color white = Color(0xFFFFFFFF);
  static const Color whiteSoft = Color(0xCCFFFFFF);
  static const Color whiteFaint = Color(0x14FFFFFF);
  static const Color whiteBorder = Color(0x1AFFFFFF);




  static const Color inkSoft     = Color(0x181A1917);

  // Drawer bg — very slightly warmer than page
  static const Color drawerBg    = Color(0xFFF4F3F0);

  // Unread dot — dark, not colorful
  static const Color unreadDot   = Color(0xFF1A1917);

  // Notification type tints (all very desaturated)
  static const Color typeScore   = Color(0xFFEDECE9);
  static const Color typeTip     = Color(0xFFEDECE9);
  static const Color typeSystem  = Color(0xFFEDECE9);

  static const accent = Color(0xFF1A1A2E);
  static const accentSoft = Color(0xFFE8E8F0);
  static const inkSubtle = Color(0xFFA8A7A4);
  // Score badge colours
  static const scoreHigh = Color(0xFF34C759);
    static const inkMuted = Color(0xFF6B6A67);

  static const scoreMid = Color(0xFFFF9F0A);
  static const scoreLow = Color(0xFFFF453A);

  // Misc
  static const border = Color(0xFFE5E4E0);
  static const saved = Color(0xFF1A1A2E);

  // saved screen
  static const bg1        = Color(0xFF0E0D0F);
  static const bg2        = Color(0xFF141218);
  static const bg3        = Color(0xFF1A1520);
  static const glassWhite = Color(0x14FFFFFF);
  static const glassBorder = Color(0x1AFFFFFF);
  static const savedRed   = Color(0xFFE8645A);
  static const cardBg     = Color(0xFF1C1A22);
  static const cardBorder = Color(0xFF2A2730);
}


